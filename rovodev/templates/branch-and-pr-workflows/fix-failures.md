Check the README if one exists to understand how to run checks locally. Use the error output below if provided; otherwise, run the build and test suite to identify failures. Resolve the root cause with a clean, architectural solution. Ensure all checks pass and that no existing functionality is regressed. Do not use brittle workarounds, error suppression, or modify valid test expectations.

{{error_output}}
